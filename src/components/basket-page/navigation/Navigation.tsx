import { NavWrapper, MenuIcon, Logo, LockIcon } from "./Navigation.css";

export const Navigation = (): JSX.Element => (
  <NavWrapper>
    <MenuIcon></MenuIcon>
    <Logo>LOVEM</Logo>
    <LockIcon></LockIcon>
  </NavWrapper>
);
